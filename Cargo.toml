[package]
name = "qol-config"
version = "0.1.0"
edition = "2021"
description = "Config discovery and loading for qol-tools"
license = "MIT"

[dependencies]
dirs = "6.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
